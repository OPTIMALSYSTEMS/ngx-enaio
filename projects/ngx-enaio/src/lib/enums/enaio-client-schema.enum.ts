/**
 * enaio child schema
 */
export enum EnaioChildSchema {
  /** only register child's */
  REG = 'REG',
  /** only document child's */
  DOC = 'DOC',
  /** only defined child's */
  DEF = 'DEF',
  /** All all child's */
  ALL = 'ALL'
}
